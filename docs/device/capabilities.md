# Device Capabilities

Capabilities describe what a device or service can legitimately support.

Capability detection does not grant authorization.

Capability detected
→ Policy evaluation
→ Authorization
→ Entitlement

A capability alone must never be interpreted as permission to perform an
operation.

Unknown capabilities must be handled conservatively.
