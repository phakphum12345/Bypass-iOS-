# Device Identity

Device identity is an input to the security pipeline.

Identity information must be:

- validated
- associated with a request
- correlated with evidence
- treated as untrusted until verified

The client may display identity information, but identity authority belongs
to the server-side device service.
